apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "featurologists.fullname" . }}-test-connection"
  labels:
    {{- include "featurologists.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "featurologists.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
